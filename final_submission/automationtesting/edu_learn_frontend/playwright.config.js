// playwright.config.js
const config = {
    use: {
      headless: false, // Show the browser window for debugging
      slowMo: 500, // Adds a 500ms delay after each action
    },
  };
  
  module.exports = config;
  